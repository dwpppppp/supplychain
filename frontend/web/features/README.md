# Features

这里是正式前端的业务主目录，按页面域拆分，而不是按组件种类堆叠。

- `auth/`：登录、认证、用户会话
- `overview/`：视频与知识图谱
- `assistant/`：数字人、AI 问答、历史会话
- `course/`：教学文档、划词问答、笔记批注
- `training/`：实训案例、步骤流程、评分报告

规则只有一条：页面专属逻辑优先留在自己的 feature 里，不要回流到全局目录。

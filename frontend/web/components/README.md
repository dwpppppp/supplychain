# Components

这里仅保留真正跨页面复用的组件，不再把页面专属模块塞进公共组件目录。

- `ui/`
  放按钮、输入框、面板、弹窗这类基础组件。
- `layout/`
  放顶栏、侧栏、平台壳子这类跨页面结构组件。
- `shared/`
  放空状态、标签、状态块、分页头这类轻业务公共块。

不要把 `assistant`、`course`、`training` 这类页面专属组件直接堆回 `components/`。
